<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="estate_property_search_view" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" filter_domain="[('name','ilike',self)]"/>
                <field name="state" invisible="1"/>
                <field name="postcode" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="living_area" filter_domain="[('living_area','>=',self)]"/>
                <field name="facades" />
                <field name="property_type_id" />
                <field name="tag_ids" />
                <separator/>
                <filter name="filter_available" string="Available" domain="[('state','in',('new','received','accepted'))]"/>
                <filter name="filter_new_properties" string="New Properties" domain="[('state', '=', 'new')]"/>
                <filter name="filter_offered_received" string="Offer Received" domain="[('state', '=', 'received')]"/>
                <separator/>
                <filter name="group_by_postcode" string="Postcode" context="{'group_by': 'postcode'}"/>
            </search>
        </field>
    </record>

    <record id="estate_property_tree_view" model="ir.ui.view">
        <field name="name">estate.property.view.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list decoration-success="state == 'received'" decoration-bf="state == 'accepted'" decoration-muted="state == 'sold'" decoration-danger="state == 'canceled'">
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="estate_property_form_view" model="ir.ui.view">
        <field name="name">estate.property.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                        <button string="Sold" name="action_sold" type="object" class="oe_highlight" invisible="state in ('sold', 'canceled')"/>
                        <button string="Cancel" name="action_cancel" type="object" invisible="state in ('sold', 'canceled')"/>
                        <field name="state" widget="statusbar" statusbar_invisible="new,offer_recieved,offer_accepted,sold" class="oe_rigth"/>
                </header>
                <sheet>
                    <group name="Name">
                        <div class="oe_title">
                            <label for="name" string="Name"/>
                            <h1>
                                <field name="name" placeholder="e.g. House Number 1"/>
                            </h1>
                        </div>
                    </group>
                    <group>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    </group>
                    <group>
                        <group>
                            <field name="property_type_id"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="currency_id" />
                            <field name="expected_price" widget="monetary"/>
                            <field name="best_offer" widget="monetary"/>
                            <field name="selling_price" widget="monetary"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="living_area"/>
                                <field name="garage"/>
                            </group>
                            <group>
                                <group>
                                    <field name="garden"/>
                                    <field name="garden_area" invisible="not garden"/>
                                </group>
                                <group>
                                    <field name="garden_orientation" widget="radio" invisible="not garden"/>
                                </group>
                            </group>
                            <group>
                                <field name="total_area"/>
                                <field name="facades"/>
                                <field name="bedrooms"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids">
                                <list string="Offers" editable="top" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                                    <!-- <field name="property_id" readonly="1"/> -->
                                    <field name="price" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                    <field name="partner_id"/>
                                    <field name="validity" />
                                    <field name="date_deadline" column-invisible="parent.status == 'sold'"/>
                                    <field name="status" optional="hide" widget="badge"/>
                                    <button string="Accept" name="action_accept" type="object" icon="fa-check" invisible="status in ('accepted', 'refused')"/>
                                    <button string="Refuse" name="action_refuse" type="object" icon="fa-close" invisible="status in ('accepted', 'refused')"/>
                                </list>
                                <form>
                                    <group>
                                        <field name="partner_id"/>
                                        <field name="price" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                        <field name="status" widget="radio"/>
                                        <field name="validity"/>
                                        <field name="date_deadline"/>
                                    </group>
                                </form>
                            </field>
                        </page>
                        <page string="Other Information">
                            <group>
                                <field name="buyer_id"/>
                                <field name="salesperson_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="estate_property_view_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_filter_available': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first property
            </p>
        </field>
    </record>
    
</odoo>
